{% extends 'base_admin.html.twig' %}

{% block body %}

	<div class="left-margin-admin">
	<a href="{{path('add_programmation')}}"><button type="submit" class="btn btn-lg btn-primary mb-2 float-right">Ajouter</button></a>

	<table class="table table-striped table-hover" id="atelier-tableau">
		<thead>
			<tr>
				<th scope="col">Id</th>
				<th scope="col">Créneau</th>
				<th scope="col">Atelier</th>
				<th scope="col">Editer</th>
				<th scope="col">Supprimer</th>
			</tr>
		</thead>
		<tbody>
            {% for programmation in programmations %}
                <tr>
                    <td>{{ programmation.id }}</td>
                    <td>{{ programmation.creneau.horaire|date("d/m/Y/ à H:i:s") }}</td>
                    <td>{{ programmation.atelier.category.categoryname }}</td>
                    <td><a href="{{ path('update_programmation', {'id' : programmation.id}) }}"><img class="admin-imgg" src="/assets/edit.png"></a></td>
                    <td><a href="{{ path('delete_programmation', {'id' : programmation.id}) }}"><img class="admin-imgg" src="/assets/trash.png"></a></td>
                </tr>
            {% endfor %}
		</tbody>
	</table>
	</div>
{% endblock %}